name: codebuilddeploy

networks:
  development:
    name: codebuilddeploy
    driver: bridge

services:
  codebuilddeploy:
    image: codebuilddeploy.azurecr.io/codebuilddeploy:latest
    build:
      context: .
      dockerfile: ./src/CodeBuildDeploy/Dockerfile
      target: final
      args:
        FEED_ACCESSTOKEN: $FEED_ACCESSTOKEN
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: https://+:5000
      ASPNETCORE_Kestrel__Certificates__Default__Password: $CERT_PASSWORD
      ASPNETCORE_Kestrel__Certificates__Default__Path: /https/code-build-deploy.pfx
    volumes:
      - ~/.aspnet/https:/https:ro
    ports:
      - 5000:5000
    networks:
      - development
    container_name: codebuilddeploy
    hostname: codebuilddeploy